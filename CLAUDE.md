# Claude Code Coordination

Claude Code owns `apps/frontend/`.

Backend and sensor-agent changes must coordinate with the frontend through:

- `/openapi.json`
- generated artifacts in `packages/shared-types/`

Do not edit `apps/frontend/` from Codex-owned milestones.
