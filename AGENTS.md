# Agent Ownership

Codex owns:

- `apps/backend/`
- `apps/sensor-agent/`
- `packages/shared-types/`
- `infra/`
- `.github/workflows/`
- `docs/`
- root guardrails and release files

Claude Code owns:

- `apps/frontend/`

Shared contract changes must flow through OpenAPI generation and committed shared-type artifacts.
